retargeting:
  type: position
  urdf_path: tesollo_dg3f_right.urdf

  target_link_names: ["right_F1_TIP", "right_F2_TIP", "right_F3_TIP"]

  target_link_human_indices: [0, 1, 2]

  # Fixed base: fingertip positions must be in the hand base frame (delto_base_link)
  add_dummy_free_joint: false

  low_pass_alpha: 1.0
